# API-контракты: Массовые операции над задачами

**Функция**: 003-multiple-operations | **Дата**: 2026-05-23

## Существующие эндпоинты (без изменений)

| Метод | Путь | Описание |
|-------|------|----------|
| GET | /api/tasks?date=&assignees= | Получить задачи по дате |
| GET | /api/tasks/{id} | Получить задачу по ID |
| GET | /api/tasks/assignees | Получить список исполнителей |
| POST | /api/tasks | Создать задачу |
| PUT | /api/tasks/{id} | Обновить задачу |
| DELETE | /api/tasks/{id} | Удалить задачу |
| POST | /api/tasks/bulk/delete | Массовое удаление |
| POST | /api/tasks/bulk/move | Массовое изменение даты |

## Новый эндпоинт

### POST /api/tasks/bulk/move-incomplete-to-tomorrow

Переносит все невыполненные задачи (Status != Done) на завтрашний день.

**Запрос**: Тело отсутствует (параметры не требуются)

```json
POST /api/tasks/bulk/move-incomplete-to-tomorrow
Content-Type: application/json
```

**Ответ 200 OK**:

```json
{
  "moved": 5,
  "targetDate": "2026-05-24T00:00:00Z"
}
```

| Поле | Тип | Описание |
|------|-----|----------|
| moved | int | Количество перенесённых задач. 0, если нет невыполненных задач |
| targetDate | DateTime | Целевая дата (завтра от текущей даты сервера) |

**Ответ 200 OK (нет задач для переноса)**:

```json
{
  "moved": 0,
  "targetDate": "2026-05-24T00:00:00Z"
}
```

**Определение «завтра»**: `DateTime.UtcNow.Date.AddDays(1)`. Сервер определяет завтрашнюю дату на основе UTC.

**Ответ 500 Internal Server Error**:

```json
{
  "type": "https://tools.ietf.org/html/rfc9110#section-15.6.1",
  "title": "Internal Server Error",
  "status": 500,
  "detail": "An unexpected error occurred while processing bulk operation"
}
```

| Условие | Код | Описание |
|---------|-----|----------|
| Внутренняя ошибка сервера при выполнении операции | 500 | Сервер не смог выполнить массовую операцию. Клиент ДОЛЖЕН отобразить сообщение об ошибке и сохранить текущее состояние без изменений |

**Идемпотентность**: Повторный вызов с тем же параметром `tomorrow` (та же дата) не приводит к дополнительным изменениям — задачи, уже имеющие целевую дату, не затрагиваются повторно.

## Существующие эндпоинты — изменения поведения

### POST /api/tasks/bulk/delete

**Изменений нет.** Существующий контракт остаётся без изменений.

**Обработка ошибок**: При ошибке сервер возвращает 500. Клиент ДОЛЖЕН отобразить сообщение об ошибке и сохранить текущее состояние задач без изменений.

### POST /api/tasks/bulk/move

**Изменений нет.** Существующий контракт остаётся без изменений.

**Обработка ошибок**: При ошибке сервер возвращает 500. Клиент ДОЛЖЕН отобразить сообщение об ошибке и сохранить текущее состояние задач без изменений.

## Ограничения

- Операция «Перенести на завтра» не принимает список IDs — сервер сам находит все невыполненные задачи
- Операция не требует аутентификации (приложение не имеет системы авторизации)
- Нет ограничений на количество задач — переносятся все невыполненные задачи во всём хранилище
- Операция не возвращает список перенесённых IDs — только количество