# Чек-лист ревью задач: Массовые операции над задачами

**Назначение**: Стандартный аудит качества задач — проверка полноты покрытия требований, ясности формулировок и прослеживаемости задач к спецификации
**Создано**: 2026-05-23
**Обновлено**: 2026-05-23 (устранение выявленных проблем)
**Функция**: [spec.md](../spec.md) | [plan.md](../plan.md) | [tasks.md](../tasks.md)

**Глубина**: Стандартная
**Аудитория**: Рецензент (gate-ревью перед реализацией)
**Акцент**: Полнота покрытия требований + качество формулировок

## Полнота покрытия требований

- [x] CHK047 Покрыт ли FR-005 (все операции без подтверждений) полностью — удалён ли `window.confirm` также из `handleBulkMove`, или только из `handleBulkDelete`? [Coverage, Spec §FR-005] → **Уточнено: `handleBulkMove` НЕ использует `window.confirm` — только `handleBulkDelete`. T004 переформулирована с явной ссылкой на FR-005**
- [x] CHK047a Покрыт ли FR-023 (пустое состояние при удалении всех задач) какой-либо задачей, или это считается существующим поведением? [Gap, Spec §FR-023] → **Добавлена T016 — проверить существующую обработку empty state, при необходимости добавить**
- [x] CHK048 Покрыт ли FR-017 (обратная связь о количестве затронутых задач) в контексте каждой операции, или только в фазе доработки (T015)? [Completeness, Spec §FR-017] → **Обратная связь добавлена в каждую US: T005 (US1), T007 (US2), T012 (US3). Заводной механизм — T002 (Toast) в фундаменте**
- [x] CHK049 Определена ли задача для обработки пустого результата «Перенести на завтра» (FR-020) с указанием конкретного UI-механизма — toast, inline-сообщение, или что-то иное? [Clarity, Spec §FR-020] → **Добавлен T002 (Toast.tsx) как механизм уведомлений. T012 явно ссылается на Toast для FR-020**
- [x] CHK050 Есть ли задача для проверки/обеспечения FR-015 (отмена выбора даты в пикере не применяет изменения) в существующем BulkActionsPanel? [Coverage, Spec §FR-015] → **Проверено: `handleBulkMove` не выполняется при закрытии пикера без выбора — `setShowMoveForm(false)` просто скрывает форму. FR-015 уже соблюдается**
- [x] CHK051 Есть ли задача для проверки/обеспечения FR-016 (порядок задач в целевом дне сохраняется после bulk-move)? [Coverage, Spec §FR-016] → **Проверено: `bulkMove` в taskStore удаляет перемещённые задачи из локального стейта и перезагружает список — порядок определяется сервером. FR-016 соблюдается на уровне backend**
- [x] CHK052 Покрыт ли FR-013 (сообщение об ошибке при сбое API) для ВСЕХ операций, или только для «Перенести на завтра» (T012)? [Completeness, Spec §FR-013] → **FR-013 теперь покрыт для всех операций: T006 (bulkDelete), T008 (bulkMove), T012 (moveIncompleteToTomorrow)**

## Ясность формулировок задач

- [x] CHK053 Достаточно ли конкретна T001 («проверить существующую структуру проекта») для реализации, или она должна содержать конкретные шаги верификации? [Clarity] → **Удалена — проект уже существует и работает, верификация не нужна как отдельная задача**
- [x] CHK054 Является ли T005 задачей на реализацию («проверить, что выделение снимается») или на верификацию? [Clarity] → **Исходная T005 удалена — `bulkDelete` и `bulkMove` уже снимают выделение через `selectedTaskIds: []`. FR-011 соблюдается без изменений**
- [x] CHK055 Содержит ли T006 достаточно контекста для создания Command record и Handler? [Clarity] → **T009 (бывшая T006) переформулирована с явной ссылкой на сигнатуру из data-model.md и логику обработки**
- [x] CHK056 Уточнён ли тип уведомления «Нет задач для переноса» — новый UI-компонент, toast, или существующий механизм? [Clarity, Spec §FR-020] → **Добавлен T002 (Toast.tsx) — простой механизм уведомлений. Все задачи по уведомлениям ссылаются на T002**
- [x] CHK057 Определён ли конкретный механизм debounce для предотвращения повторных нажатий (FR-021)? [Clarity, Spec §FR-021] → **T013 явно указывает `disabled` на время запроса, T012 описывает блокировку кнопки**
- [x] CHK058 Указан ли конкретный объём работы по клавиатурной доступности? [Clarity, Spec §FR-022] → **T015 переформулирована: «добавить Tab/Space/Enter, проверить существующий aria-label»**

## Согласованность задач с артефактами

- [x] CHK059 Согласованы ли пути файлов в задачах с фактической структурой проекта? [Consistency] → **Проверено: TaskCheckbox.tsx находится в `Board/TaskCheckbox.tsx`. Все пути в tasks.md согласованы с фактической структурой**
- [x] CHK060 Согласован ли Command record (без параметров) с contracts/api.md? [Consistency, contracts/api.md] → **T009 описывает Command без параметров, что соответствует POST без тела в contracts/api.md**
- [x] CHK061 Согласовано ли поведение при `moved === 0` с US-3.2 из spec.md? [Consistency, Spec §US-3.2] → **T012 явно указывает «Нет задач для переноса» — согласуется с US-3.2**
- [x] CHK062 Согласована ли сигнатура `MoveIncompleteToTomorrowAsync` с data-model.md? [Consistency, data-model.md] → **T001 явно указывает сигнатуру из data-model.md: `DateTime tomorrow, CancellationToken ct`**

## Прослеживаемость требований

- [x] CHK063 Можно ли проследить каждую задачу US3 к конкретным FR из спецификации? [Traceability] → **Все задачи US3 теперь содержат ссылки на FR: T009 (FR-019), T012 (FR-017, FR-020, FR-013, FR-021), T013 (FR-019, FR-021)**
- [x] CHK064 Имеет ли задача по удалению window.confirm явную ссылку на FR-005? [Traceability] → **T004 содержит явную ссылку: «(FR-005: все массовые операции без подтверждений)»**
- [x] CHK065 Покрывает ли обратная связь (FR-017) все три типа операций? [Traceability, Spec §FR-017] → **T005 (US1: удаление), T007 (US2: перемещение), T012 (US3: перенос на завтра) — каждая операция имеет свою задачу по обратной связи**

## Покрытие граничных случаев

- [x] CHK066 Покрыт ли граничный случай «все задачи выполнены → нажатие Перенести на завтра»? [Edge Case, Spec §US-3.2] → **T012 явно обрабатывает `moved === 0` с уведомлением «Нет задач для переноса»**
- [x] CHK067 Покрыт ли граничный случай «повторное нажатие Перенести на завтра» (FR-021)? [Edge Case, Spec §FR-021] → **T013: кнопка блокируется (`disabled`) на время выполнения запроса. T012: store устанавливает `isLoading` для предотвращения повторных вызовов**
- [x] CHK068 Покрыт ли граничный случай «массовое удаление ВСЕХ задач» (FR-023)? [Edge Case, Spec §FR-023] → **Добавлена T016 — проверить существующую обработку empty state, при необходимости добавить условие рендеринга**

## Качество зависимостей и порядка

- [x] CHK069 Является ли Фаза 1 (T001) необходимой для данной функции? [Completeness] → **Исходная T001 (проверка структуры) удалена. Новая Фаза 1 содержит только существенные задачи: метод репозитория, механизм уведомлений, реализация метода**
- [x] CHK070 Корректно ли указаны параллельные возможности — может ли frontend-задача выполняться параллельно с backend-задачами? [Consistency] → **Да: T009 (backend) и T011 (frontend taskApi) находятся в разных проектах и могут выполняться параллельно**
- [x] CHK071 Обосновано ли отсутствие задач на тестирование? [Completeness] → **Обосновано: спецификация не запрашивает TDD-подход. Тестовые директории существуют, но задачи на тестирование не включены как обязательные**

## Неоднозначности

- [x] CHK072 Почему T005 (US2) — это только «проверить», а не «обеспечить и проверить»? [Ambiguity] → **Исходная T005 удалена — `bulkMove` уже снимает выделение через `selectedTaskIds: []`. FR-011 соблюдается без изменений. Новые T007 и T008 — задачи на реализацию (обратная связь и обработка ошибок)**
- [x] CHK073 Является ли T014 (доступность с клавиатуры) задачей на реализацию или на верификацию? [Ambiguity] → **T015 переформулирована как задача на реализацию: «добавить Tab/Space/Enter, проверить aria-label»**
- [x] CHK074 Должна ли задача по стилям быть частью задачи по кнопке, или это оправданно отдельная задача? [Clarity] → **Оправданно как отдельная задача: T013 (Header.tsx) и T014 (index.css) — разные файлы, могут выполняться параллельно**

## Заметки

- Отмечайте элементы как выполненные: `[x]`
- Добавляйте комментарии или выводы прямо в текст
- Элементы нумеруются последовательно (продолжение от CHK046 из bulk-operations.md)
- Ссылки на спецификацию: `Spec §FR-XXX`, `Spec §US-X`, `Spec §Граничные случаи`
- Ссылки на артефакты: `Research §Решение N`, `Data Model`, `Contracts`, `tasks.md §Фаза N`
- `[Gap]` — требование отсутствует в задачах
- `[Clarity]` — формулировка задачи допускает неоднозначное толкование
- `[Consistency]` — задача не согласована с другими артефактами

### Сводка исправлений

Все 28 элементов чеклиста (CHK047–CHK074) разрешены. Ключевые изменения в tasks.md:

1. **Добавлен T002 (Toast.tsx)** — механизм уведомлений для FR-017 и FR-020
2. **FR-005 уточнён** — только `handleBulkDelete` использует `window.confirm`, `handleBulkMove` — нет
3. **FR-013 покрыт для всех операций** — T006 (US1), T008 (US2), T012 (US3)
4. **FR-017 (обратная связь) встроен в каждую US** — T005 (US1), T007 (US2), T012 (US3)
5. **FR-020 (сообщение «Нет задач»)** — T012 использует механизм Toast из T002
6. **FR-021 (debounce)** — T013: `disabled` на время запроса
7. **FR-023 (empty state)** — добавлена T016
8. **FR-022 (клавиатурная доступность)** — T015 переформулирована как задача на реализацию
9. **Удалены «проверочные» задачи** — T005 (бывшая) заменена на T007/T008 (реализация)
10. **Удалена T001 (проверка структуры)** — проект уже существует